# Research evidence

`skill-lifecycle-native-20260920.json` retains a historical analysis export of selected skill lifecycle functions. Its `database` field records the original local analysis database path; that database is not distributed here.

Decompiler output is a research aid, not an equivalence certificate. Read it together with the dated audits and the current C++ implementation. Additional native comparison fixtures referenced by tests live outside this repository; see [BUILDING.md](../BUILDING.md).
